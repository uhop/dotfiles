# Absolute: et's ssh-config parser resolves a relative Include against the cwd, then aborts
Include ~/.ssh/config.d/*

Host *
  AddKeysToAgent yes
  ControlMaster auto
  ControlPath ~/.ssh/sockets/%r@%h-%p
  ControlPersist 5m

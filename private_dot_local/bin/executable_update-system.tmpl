#!/usr/bin/env bash

echo Run sudo apt...
sudo apt update && sudo apt upgrade
echo Run sudo snap...
sudo snap refresh
{{ if .hasGui -}}
echo Run flatpak...
flatpak update
{{- end }}
echo Run brew...
brew update && brew upgrade

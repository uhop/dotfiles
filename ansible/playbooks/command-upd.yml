---
- name: Run upd
  hosts: Linux
  gather_facts: no
  vars:
    upd_flags: '-y'
  tasks:
    - name: Execute upd
      ansible.builtin.command: 'upd {{ upd_flags }}'
      register: upd_result
      changed_when: true
      failed_when: upd_result.rc != 0
